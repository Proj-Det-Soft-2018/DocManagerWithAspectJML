CREATE TABLE interessados_compra (
	id BIGINT NOT NULL AUTO_INCREMENT,
  razao VARCHAR(255) NOT NULL,
  cnpj VARCHAR(14) NOT NULL UNIQUE,
  nome_resp VARCHAR(255) NOT NULL,
  cpf_resp VARCHAR (11) NOT NULL UNIQUE,
  contato VARCHAR(20),
  PRIMARY KEY (id),
	UNIQUE KEY (cnpj)
) ENGINE = InnoDB;